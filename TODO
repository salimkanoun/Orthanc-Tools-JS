Front : 
- Faire que le select all select tout les non filtre de toutes les pages ? => difficulté....
- Sous colonne modificatrice pour l'ensemble de la colonne quand table Query => Reste a faire le deselect avant edition
- retrieve et export button pas forcement à connecter
    - Transformer export en dowload button pour download vers client
    - Le reserver que pour le unique query ?
    
- Query level series avec expendable rows on click (declanche retrieve des series)
    - Ne pas charger les series data si deja chargées dans le redux
    - Reste a gerer le Retrieve / export button pour agir sur Study et Serie level
    - Bug selection serie level
    - Gerer la selection pour le robot...

- Interface Suivi robot (pour un utilisateur)
    -   List de detail requete
    -   Editer ou effacer requete
    -   Statut de chaque requete : Idle, Pending, Retrieved, error ? + progress bar
    -   Si requete faite => 
            - Vers outil Anon ou vers Export / telechargement via basket ?
            - Liens vers visualisation dans OHIF
    -   Export rapport Query / Retrieve

- Interface suivi des robots (tous) pour un administrateur
    -   Validation d'un robot avant execution (fonction preset plateforme)
    -   Acces au robot de chaque utilisateur

- Spliter firstName, LastName dans toutes les tables query / results ?

- Settings 
    - Splitter les "domaines" dans des tabs avec refresh auto (plus de refresh)
    - AutoQuery : 
        - Filtre sur le niveau Series => Contains et equal sur modality et series description
            - Lors de creation robot + edition sur interface utilisateur
        - Nombre limit de query si < limit => Retrieve auto

- Admin Panel : 
    - Declaration des AET ajout stockage dans la base de données ?
    - Echo des AETs

+ tard  : 
- historique des query dans BDD pour relancer une ancienne requete
- Manual Query avec le formulaire manuel avec un duplicat de la table results

Back :
- Securiser une seule retrieve par query lors execution du robot
- Bug pipe des zip >2GB => Devrait etre OK
- Midelware securite acces robot
- PassportJS pour stateless
- UnitTests
- Envisager de switcher de Request à Got (damn...)

Tutos : 

Sequelize
https://stackabuse.com/using-sequelize-js-and-sqlite-in-an-express-js-app/